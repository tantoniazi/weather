exit
response = self.class.get("/weather", query: {
                                            q: "#{city},BR",
                                            units: "metric",
                                            lang: "pt_br",
                                            appid: @api_key,
                                          })
viacep = HTTParty.get("https://viacep.com.br/ws/#{@zip_code}/json/")
    city = viacep["localidade"]
response
n
c!
WeatherService.new(zipcode, current_user).forecast
zipcodeWeatherService.new(zipcode, current_user).forecast
zipcode
WeatherService.new(zipcode, current_user)
WeatherService.new(zipcode, current_user)WeatherService.new(zipcode, current_user)
